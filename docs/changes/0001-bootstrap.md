# Change 0001: Bootstrap clean-room Go agent

## Summary

Created the initial clean-room repository structure for `ccagent`, including configuration, auth persistence, workspace tooling, local git helpers, transcript logging, docs, tests, and CI.

## Why

The project needed a production-oriented baseline that is legally clean, operationally auditable, and ready for iterative feature work.
